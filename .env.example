# Gemini API key for AI metadata generation (optional for local dev).
# Get your key at: https://aistudio.google.com/apikey
# For production / GitHub Pages, set the key in the in-app Settings instead.
GEMINI_API_KEY=
