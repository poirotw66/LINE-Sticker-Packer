/** localStorage key for user-entered Gemini API key (browser only). */
export const GEMINI_API_KEY_STORAGE_KEY = 'line-sticker-packer-gemini-api-key';

/** localStorage key for selected Gemini model (browser only). */
export const GEMINI_MODEL_STORAGE_KEY = 'line-sticker-packer-gemini-model';
